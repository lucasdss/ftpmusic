<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".FtpmusicApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="FTP Music"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ftpmusic"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Media playback service for Cast + Android Auto -->
        <service
            android:name=".playback.MediaService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
            </intent-filter>
        </service>

        <!-- Cast SDK: auto-reconnect after app restart / process death -->
        <service
            android:name="com.google.android.gms.cast.framework.ReconnectionService"
            android:exported="false" />

        <!-- Media3: Output Switcher for notification/lock screen device transfer -->
        <receiver
            android:name="androidx.mediarouter.media.MediaTransferReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.cast.TRANSFER_MEDIA" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings tile for playback control -->
        <service
            android:name=".playback.PlaybackTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="FTP Music"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.lucasdss.ftpmusic.app.cast.CastOptionsProvider" />

        <!-- Notification action button handler -->
        <receiver
            android:name=".playback.MediaActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.lucasdss.ftpmusic.app.MEDIA_ACTION" />
            </intent-filter>
        </receiver>

        <!-- E6: media button events from Bluetooth / headset / hardware keys.
             Media3's receiver forwards them to the MediaSession even when the
             service is not running (starts it via the session token). -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
